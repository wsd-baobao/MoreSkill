<!--
  Planning Spec
  Generated by: Qoder /planning skill
  Date: 2026-08-16
  Status: Draft
-->

# Plan: 修复技能系统高危bug并消除重复代码，使技能系统稳定、可维护、行为兼容

**Complexity: Medium** — 跨9个技能子系统的多文件修复与重构；接口需保持稳定，行为必须兼容。

## Goal

让MoreSkill技能系统在专用服务器上稳定运行、数据持久化正确、装备/交易/掉落逻辑无崩溃与丢失，同时消除重复注册表和调试噪音，保持现有游戏机制不变。

## Deliverables

- **修复**: `FishingPoolManager` 移除静态块中的客户端类 `Minecraft` 引用，附魔物品改为服务端安全生成 → 消除专用服务器崩溃
- **修复**: `MiningHandler.onPlayerBreakBlock` 空掉落列表判空 + 额外掉落只生成副本 → 消除崩溃与双倍掉落
- **修复**: `FishingHandler.getPlayerPool` 判空/computeIfAbsent → 消除NPE
- **修复**: `SmithingHandler` 换装时按槽位精确移除/恢复修饰器 → 保留其他槽位锻造加成
- **修复**: `TradingHandler.adjustTradeCosts` 只对costA计算一次折扣 → 双物品交易正确
- **修复**: 统一9个技能的升级算法（保留溢出经验 + while循环）→ 经验不丢失
- **修复**: `Trading.getExpForNextLevel` int溢出 → 用long计算并封顶
- **修复**: `MiningHandler.onPlayerTick` 增加isClientSide检查 → 客户端不执行服务端逻辑
- **修复**: `FarmingHandler`/`SmithingHandler` static Map 在登出时清理 → 消除内存泄漏
- **重构**: `CapabilityEventHandler` 单一技能注册表（SKILL_ENTRIES驱动 attach/load/save/sync）→ 新增技能只需改一处
- **清理**: 18处 `System.out.println` → logger.debug/删除；7个未使用导入；死代码字段
- **文档**: 优化spec与交付说明

**Out of scope:**
- 9个Data类抽象为公共基类/接口（行为面大，建议单独迭代）
- SkillPanelScreen 硬编码数组重构（UI行为面，建议单独迭代）
- FishingPoolManager 440行静态物品池数据驱动化（重构风险高）
- Magic 技能完整实现或删除（需用户决策）
- Cooking.java/Farming.java 乱码注释转码（纯文本修复，低优先级）

## Acceptance Criteria

```
AC-1  [崩溃修复]  Given 专用服务器启动, When FishingPoolManager 被加载, Then 不引用 Minecraft 客户端类、不崩溃；附魔物品在服务端事件中安全生成。

AC-2  [崩溃修复]  Given 玩家用不匹配工具挖掘, When 方块 getDrops 返回空列表, Then MiningHandler 不抛 IndexOutOfBoundsException；额外掉落仅为副本且不双倍。

AC-3  [数据正确]  Given 玩家未初始化钓鱼池, When FishingHandler 处理钓鱼事件, Then 不抛 NPE，正常初始化玩家池。

AC-4  [数据正确]  Given 玩家穿戴两件以上已锻造装备, When 更换其中一件, Then 其他槽位的锻造加成保留不丢失。

AC-5  [数据正确]  Given 双物品交易（costA+costB）, When 玩家等级打折, Then 两件物品价格都正确折扣（不再互相覆盖）。

AC-6  [经验保留]  Given 技能升级时经验溢出, When 升级完成, Then 溢出经验保留到下一级（9个技能统一算法）。

AC-7  [内存/并发]  Given 玩家登出, When 触发登出事件, Then Farming/Smithing 的 static Map 条目被清理。

AC-8  [一致性]    Given 技能代码库, When 静态检查, Then 无 System.out.println、无未使用 MoreSkill 导入、注册表单一来源。
```

## Implementation Path

1. **FishingPoolManager 服务端安全化** — 静态块中移除 `Minecraft.getInstance()`，附魔逻辑改为在 `FishingHandler` 事件中惰性生成（用 `ServerLevel.random`）→ files: `skill/fishing/FishingPoolManager.java`, `FishingHandler.java` → verify: grep 确认无 Minecraft 引用
2. **MiningHandler 掉落修复** — 空列表判空、额外掉落 `copy()` 副本、清理 `(ServerLevel)` 强转 → files: `skill/mining/MiningHandler.java` → verify: 逻辑审查
3. **FishingHandler NPE 修复** — `getPlayerPool` 用 `computeIfAbsent` → files: `skill/fishing/FishingHandler.java` → verify: 审查
4. **SmithingHandler 换装修复** — `removeArmorModifiers` 按槽位移除/恢复 → files: `skill/smithing/SmithingHandler.java` → verify: 逻辑审查
5. **TradingHandler 折扣修复** — 只对 costA 计算 → files: `skill/trading/TradingHandler.java` → verify: 审查
6. **升级算法统一** — 9个Data类的 addExp 统一为"保留余量 + while循环"，Trading 用 long 计算 → files: 9个 Data 类 → verify: 审查
7. **内存泄漏与客户端检查** — Farming/Smithing 登出清理 Map；MiningHandler tick 加 isClientSide → files: 3个 Handler → verify: 审查
8. **注册表单一化** — CapabilityEventHandler 用 SKILL_ENTRIES 驱动 attach/load/save/sync → files: `skill/CapabilityEventHandler.java`, `network/SyncSkillPacket.java` → verify: 审查
9. **噪音清理** — System.out.println → logger；未使用导入；死代码 → files: 各技能文件 → verify: grep
10. **编译验证** — `./gradlew compileJava`（如环境允许）→ verify: 编译通过

## Open Questions

None.

## Assumptions

- 所有修复保持现有游戏数值/公式/机制不变，仅修bug与重构
- 升级经验统一为"保留余量+while循环"（Farming/Smithing 现有正确行为作为标准）
- Trading 经验公式保持 `100*2^level` 数值曲线，仅修复 int 溢出（用 long 计算，超 Integer.MAX_VALUE 时封顶）
- 不引入新依赖、不改变存档格式（NBT key 保持 "Level"/"Experience"）
